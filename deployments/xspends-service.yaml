apiVersion: v1
kind: Service
metadata:
  name: xspends-service
spec:
  selector:
    app: xspends
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080  # Assuming your application runs on port 8080 inside the container
  type: NodePort
